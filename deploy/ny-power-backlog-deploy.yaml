apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: ny-power-backlog
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: backlog
    spec:
      volumes:
      - name: secret-volume
        secret:
          secretName: mqtt-pump-secret
      containers:
      - name: ny-power-pump
        image: registry.ng.bluemix.net/sdague/ny-power-backlog
        imagePullPolicy: Always
        volumeMounts:
          - name: secret-volume
            readOnly: true
            mountPath: "/etc/secret-volume"
        env:
        - name: INFLUXDB_HOST
          value: ny-power-influxdb
